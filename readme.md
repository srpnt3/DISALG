The code for each process is located in [`src/algorithm.rs`](./src/algorithm.rs).

To run type`cargo run`.
Network topology can be changed in [`topology.toml`](./topology.toml), as well as whether `Start()` is sent to all or just one process.